#include <stdio.h>
#include "main.h"

/**
 * print_numbers - functions that print all numbers from 0 to 9
 *
 * Return: Returns nothing
 */
void print_numbers(void)
{
	int n;

	for (i = 48, n < 58; n++)
	{
		putchar(n);
	}
	putchar(10);
}
